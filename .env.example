# Michishirube Environment Variables Configuration
# Copy this file to .env and customize as needed

# =============================================================================
# Core Application Settings
# =============================================================================

# Server port (default: 8080)
PORT=8080

# Database file path (default: michishirube.db)
# For Docker: /data/michishirube.db (persisted volume)
# For local: ./michishirube.db or absolute path
DB_PATH=./michishirube.db

# Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# Optional: Path to YAML config file (default: config.yaml)
# CONFIG_PATH=./config.yaml

# =============================================================================
# Docker-specific Settings
# =============================================================================

# Development server port (when using docker-compose with dev profile)
DEV_PORT=8081

# Development log level
DEV_LOG_LEVEL=debug

# Development config path
# DEV_CONFIG_PATH=./config/dev.yaml

# =============================================================================
# Build Information (automatically set during builds)
# =============================================================================

# Application version (set by GoReleaser/Docker)
# APP_VERSION=v1.0.0

# Git commit hash (set automatically)
# APP_COMMIT=abc1234

# Build date (set automatically)
# APP_BUILD_DATE=2024-01-15T10:30:00Z

# Built by (docker, goreleaser, manual, etc.)
# APP_BUILT_BY=goreleaser

# =============================================================================
# Development and Testing
# =============================================================================

# Update test fixtures (for development)
# UPDATE=true

# CGO enabled/disabled (for cross-compilation)
# CGO_ENABLED=1

# =============================================================================
# Usage Examples
# =============================================================================

# Production with custom database location:
# PORT=8080
# DB_PATH=/var/lib/michishirube/production.db
# LOG_LEVEL=warn

# Development with debug logging:
# PORT=8081
# DB_PATH=./dev.db
# LOG_LEVEL=debug

# Docker production deployment:
# PORT=8080
# DB_PATH=/data/michishirube.db
# LOG_LEVEL=info